return {
  "nvim-treesitter/nvim-treesitter",
  opts = {
    ensure_installed = "all", -- install all maintained parsers
    highlight = {
      enable = true,
    },
    indent = {
      enable = true,
    },
  },
}

